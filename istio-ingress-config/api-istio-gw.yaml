apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  annotations:
  labels:
    version: v1
  name: api-gateway
spec:
  selector:
    istio: ingress
  servers:
  - hosts:
    - "*.com"
    port:
      name: http-dev
      number: 80
      protocol: HTTP